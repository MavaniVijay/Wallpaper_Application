import 'package:flutter/material.dart';

class FashionScreen extends StatelessWidget {
  static final id = "FashionScreen";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        height: 100,
        width: 200,
        color: Colors.green,
      ),
    );
  }
}
